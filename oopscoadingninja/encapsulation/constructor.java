package oopscoadingninja.encapsulation;

public class constructor {
    // constructor make the object for us
    // as we write new x();
    // in genral form it represent the function and when it in the class it actually
    // act
    // as the object
    // default(normal wala) if we the object and we print e.gx

    // then it will classname@hexadecimalvalue (i.e 0-8 and a-f)
    public class node {
        String name;
        int roll_number;

        public node(int rn) {
            name = n;
            roll_number = rn;
        }
    }

    public static void main(String[] args) {

        node c = new node("ad", 12);
        System.out.println(c.name);
    }
}
